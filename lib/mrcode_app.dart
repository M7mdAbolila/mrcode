import 'package:flutter/material.dart';

class MrcodeApp extends StatelessWidget {
  const MrcodeApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text('welcome to Mrcode .....'),
        ),
      ),
    );
  }
}
